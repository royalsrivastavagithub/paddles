local settings = {}

function settings.enter() end
function settings.exit() end
function settings.update(dt) end
function settings.draw() end
function settings.keypressed(key) end
function settings.gamepadpressed(joystick, button) end
function settings.mousepressed(x, y, button) end

return settings
